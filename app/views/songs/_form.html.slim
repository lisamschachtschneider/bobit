h2 Upload a new MP3
= form_for @song, @song.upload :html => {:multipart => true} do |f|
	div.field
	 = f.error_messages
	 = f.hidden_field :song_id

	div.field
	 = f.label :name
	 = f.text_field :name

	p
	 = f.file_field :song

	div.actions
	 = f.submit = t('.submit')

	